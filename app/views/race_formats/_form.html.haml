= form_for @race_format do |f|
  - if @race_format.errors.any?
    #error_explanation
      %h2= "#{pluralize(@race_format.errors.count, "error")} prohibited this race_format from being saved:"
      %ul
        - @race_format.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
